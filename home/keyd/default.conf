[ids]
*

[main]
# Maps capslock to escape when pressed alone, activates nav layer when held
capslock = overload(nav, esc)

# Navigation layer: caps + hjkl = arrow keys
[nav:C]
h = left
j = down
k = up
l = right

# Optional: Make escape behave normally
esc = esc